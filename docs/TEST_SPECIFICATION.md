# Test Specification

## Equipment picker cases (2026-09-03)

PICK-007: A successful picker-origin Actor operation updates only its chosen slot in an existing Appearance or Outfit pin. Failed/cancelled operations do not update pins. Preserve pin identity/model/other slots/dyes/RGB, source and Original; do not unpin or recapture. TEST_ONLY store plus existing completion predicate; existing pin comparison must see equality with the selected-slot result after completion. Native pin scan remains UAT.

User explicitly extends editing to Actor-list current equipment. PICK-006: picker binds the clicked Actor key; selection captures its current outfit and applies only the selected slot using existing single-Actor BulkOutfit service. Switching list selection cannot redirect the open picker. First Original and later Restore persist; source outfit remains unchanged. Source/Actor mode is visible in picker. Original/applied/pinned reference tables remain read-only. Test service actor edit and UI binding by static review; runtime is user-operated.

- PICK-001: Names use existing localized comparison. e9005/E9005/9005 find exact armor model 9005 across variants, a0177 finds accessory177; wrong e/a prefix does not map to a different resource family. Direct model accepts ushort range and explicit byte Variant, including unnamed models; no catalog-completeness gate on direct armor assignment.
- PICK-002: Icon or empty placeholder opens the intended slot only. Name/results show icon, name, model and variant; current selection is marked. Favorite toggle does not select an item; favorites-only filters by current slot. Close/cancel does not edit. Native UI is UNEXECUTED until user operates.
- PICK-003: Favorites survive Configuration JSON serialization, separate armor slots and facewear row IDs, and include direct unnamed model/variant. Toggle removes the exact saved choice; unrelated settings remain intact.
- PICK-004: Source equipment selection changes only requested model/variant, retaining game stains and RGB channels; facewear selection changes only Facewear row. Existing Apply receives a fixed source snapshot and preserves first Original. Source editing/adding favorites invokes no native Apply itself. Exercise BulkOutfitService with existing TEST_ONLY memory/resolver.
- PICK-005: User opens source icon, searches name and e-number, applies selected variant, saves/removes favorite and reopens after reload. Facewear name selection displays correct icon and can be Applied without color controls. Record runtime/UAT separately from managed tests and build.

## Facewear identity regression (2026-09-03)

- FACE-ID-001: Human Glasses0 model 900/variant 3 maps to Glasses row 17, even when backing row is 23; variant 4 maps to row 18. Invoke production CaptureRendered with native structs and injected model-to-row lookup. Outfit carries row 17/18, never model 900 or backing 23. No native game functions execute.
- FACE-ID-002: Rendered empty model maps to available row 0. Unknown nonzero model maps to unavailable, not a fabricated row ID or backing fallback. This availability is descriptive; do not add an Apply rejection or diagnostic control gate.
- FACE-ID-003: Source capture, Actor snapshot capture and passive native observation use the same conversion. Existing SetGlasses/temporary backing consumers continue receiving row IDs. Read-only execution-path review plus affected outfit/appearance tests.
- FACE-ID-004: User presses the existing source refresh button while wearing facewear; icon/name/model/variant appear, with no color picker. Runtime/visual remains UNEXECUTED until observed on the deployed DLL.
- FACE-ID-005: Unknown rendered facewear remains null in AppearanceData and unavailable in OutfitData, while the 10 armor slots still convert/display and managed retain/merge still works. Never supplement a backing/row ID. Test production conversion and registry merge with null facewear; update the former completeness expectation that incorrectly required this optional lookup.

## Model Search target application (2026-09-03)

TARGET-APPLY-002 completion regression: after exact native success, the Plugin completion uses that same exact resolver overload, then applies the existing success/representation predicate and passes the exact snapshot to publication and persistence. A normal resolver returning another copy must not suppress completion. Managed regression checks the service completion event, exact resolver and existing publication/persistence helpers; source review binds these to the Plugin callback. Live native/UI remains UNEXECUTED.

- TARGET-APPLY-001: lookup current target by object slot, GameObjectId and EntityId in the current registry; multiple representations of one Actor must return the targeted one. Different slot/ID/territory never resolves to another Actor. No target reports a localized message and performs no Apply. UI no longer reads selectedActorKey for this action. Field uses Target; GPose uses GPoseTarget without fallback. Managed resolver tests plus static UI/target-service trace.
- TARGET-APPLY-002: actual AppearanceApplyService queues B with the exact target Representation and canonical Actor. The coordinator resolves that exact Representation during each step, even when the normal resolver prefers another representation. Changing the game target after click does not redirect the queued Apply. If the captured representation disappears, existing cancellation applies with no fallback/second Create. TEST_ONLY resolver/native backend doubles; unchanged payload and exactly one Enable are the oracle.
- TARGET-APPLY-003 (runtime/UAT): select Actor A in plugin list, target B in FF14, apply selected model: only B changes. Change to C before the next click: C receives the next request. Clear target: localized no-target feedback, A/self unchanged. Repeat in GPose; Apply to Yourself still targets self. Native/UI observations not performed by Codex are UNEXECUTED.

## Unified explicit bulk Actor Restore (2026-09-03)

- BULK-ACTOR-RESTORE-001: production target resolver unions outfit override keys with current model-managed or pinned Actor keys, deduplicates, includes pinned and non-Human models, excludes untouched/unpinned Actors. No outfit/name filter and no pin exclusion. TEST_ONLY keys/predicates.
- BULK-ACTOR-RESTORE-002: normal BulkOutfitService scheduler dispatches an explicitly requested Actor Restore delegate once per target/frame even when no Human outfit is capturable (model-only Monster). It does not pre-capture outfit or call the legacy outfit-only restore path. The shared Plugin completion used by both buttons clears retained model/outfit, managed marker and pin then requests exactly one payload-free redraw. For outfit overrides the same Original-first path is used. Static trace plus managed scheduler/native-memory doubles, not live FF14.
- BULK-ACTOR-RESTORE-003: stored Original succeeds/fails, per-Actor delegate succeeds/fails/throws, repeated click while busy, empty targets, cancellation, logout and territory change. Original failure does not clear model/pin or redraw; redraw failure counts as bulk failure. Busy/empty requests do not overwrite the existing delegate/operation; cancelled unprocessed targets never invoke restore. No retry/fallback/rollback added. Legacy outfit Apply/Restore tests remain applicable.
- BULK-ACTOR-RESTORE-004 (runtime/UAT): mix model-only, outfit-only, combined and pinned Actors; click Bulk Restore and compare to individual Restore: original game model/outfit, cleared pins/modified markers, no reappearance on travel. Button stays enabled with zero targets and during a running operation and shows result feedback. User operates FF14; unobserved states are UNEXECUTED.

## Full appearance pins (2026-09-03)

- PIN-FULL-005: TEST_ONLY managed Human has applied HatVisible=true, game backing=false, and otherwise identical rendered fields. Feed fresh observations through ActorRegistry.DescribeRenderedAppearance and the production PinnedOutfitStore.AppearanceEquals used by the normal scan for 77 cycles: equal every cycle, so the scan's existing equality branch dispatches no Apply. Reverse both booleans as a boundary case. Changed head/equipment/weapon/customize/visor values still compare unequal, unmanaged capture keeps its raw hat flag, non-Human capture does not acquire Human metadata, and different ContentId does not match a saved pin. The helper/comparator tests do not execute FF14 or prove native shutdown safety. User runtime: pinned current model+outfit survives normal recreation, no periodic Apply storm, explicit Restore still works; restart game, log into another account, then log out without a crash. Bind observations to the deployed DLL; unobserved runtime predicates remain UNEXECUTED.

PIN-FULL-001 identity boundary: same-name/same-base distinct Actors keep distinct full pins. Source 1 (ContentId) and 3 (layout/territory) reconnect across sessions; Source 2 (owner-bearing GameObjectId) and 4 (EntityId) reconnect across representations only within the saved plugin session. Source 5/null binds only to the exact logical key/lifetime in that session. IdentityKey grouping follows the same scope. Legacy outfit-only matching remains unchanged. JSON round-trip must preserve these fields. Fresh capture must retain and compare the canonical LogicalKey from CreateSnapshot: the same slot/GameObjectId/EntityId with another lifetime yields capture-unavailable, no saved pin, no RecordModel and no pin Apply. Exercise the actual capture-descriptor and PinCurrent path; native/UI execution remains UNEXECUTED.

- PIN-FULL-001: normal pin-store capture batch with Human, Demihuman and Monster current appearances distinct from historical model/Bulk Desired; preserve every appearance field, explicit zero Head and stains through actual configuration JSON round-trip. Missing live Actor/draw object cannot yield an invented snapshot; otherwise no completeness/readback admission gate. TEST_ONLY actor/capture inputs; save once per click, replace matching pin, preserve unrelated pins, retain more than 256 entries without eviction.
- PIN-FULL-002: existing saved outfit-only pin still loads as outfit-only, with no inferred model. Individual unpin and unpin-all remove requested saved records (including absent Actors), save once, and do not modify appearance/Original/retained model. Repeat empty clear performs no save.
- PIN-FULL-003: existing pin scan uses full visible-value comparison excluding source provenance/completeness metadata; equipment/customize contents and model, scale, weapons, visor/facewear/hat changes matter. A full pin reaches normal AppearanceApplyService; legacy pin reaches the existing outfit operation. Existing pending/failure handling prevents duplicate work without adding another timer/retry/fallback. Static trace plus managed value/capture tests; FF14 dispatch is runtime-only evidence.
- PIN-FULL-004 (User/runtime): model-only, Bulk-only and combined modified Actors can individually and collectively pin the displayed model+outfit. Actor-list filters do not narrow bulk pin; count and orange state match, all-clear includes saved absent Actors and leaves current appearance intact. Pin, travel/cutscene, then explicit Restore must retain and restore correctly. Check Japanese/English labels, narrow window, no targets, and individual pin control; no UI click or FF14 operation is delegated to hidden agents. Unobserved cases remain UNEXECUTED.

## Replanned head consumer cases (2026-09-03)

- HEAD-PENDING-001: selected Human head=0 and nonzero; original setup writes another hat to pending equipment as Glamourer's nested setter does. Actual callback leaves selected head in pending data and sets only bit0 of the existing update flags before Create returns. All other pending bytes, current rendered slots, actor backing and caller DrawData remain unchanged by the callback. TEST_ONLY native buffers/original delegate exercise the product dispatch helper; they do not prove live Glamourer behavior.
- HEAD-PENDING-002: no active Create, absent Human equipment, non-Human payload (including nonempty equipment), unrelated nested Create and second setup callback must pass through with one original call and no pending-data write. Same-thread outer scope survives the unrelated Create and is cleared on exit.
- HEAD-PENDING-003: original setup returns false or throws; preserve its return/exception and never retry, reject Apply from that observation or perform a second native call. A thrown setup does not access its unfinished model. Create null/exception clears scope. Source trace covers hook Enable failure, later constructor failure and normal Dispose cleanup. No readback/completeness admission gate.
- HEAD-PENDING-004 (runtime/UAT): same hat-wearing Actor and EventNpc#1030098 with Glamourer OFF and ON; hatless NPC must remove the hat in both. Apply a hat-bearing NPC, recreate via travel/cutscene, then explicitly Restore; original equipment must remain intact. Existing logs compare requestedHead/inputHead and User checks final appearance. Each unobserved variant is UNEXECUTED. The user operates FF14. On requested removal reinstall the saved pre-callback DLL, with no automatic rollback.

All legacy local-only, arm/unarm, latest-C1/C2, one-transfer-per-key and `ARM_AFTER_COMPLETE_C` transition cases below are **HISTORICAL / SUPERSEDED**. Current transition acceptance is TRANS-001..006 and TRANS-ID-001..003. Those historical cases must not supply current PASS evidence; explicit Apply/backing/Bulk Original cases remain active.

- TRANS-ID-001: identical old raw LogicalActorKey and RepresentationKey, but different ContentId/LayoutId/owner/full GameObjectId or Character lifetime: normal persistence lookup yields no model/outfit and registry retention does not mark the replacement managed. Include Source=5 address/slot reuse after destruction. TEST_ONLY native identity/lifetime inputs; actual resolver/retention helpers.
- TRANS-ID-002: Source4→Source1, Source5→identified, identified→temporarily provisional within one living Character: bind only those observed aliases to the same Actor, then recreate with the confirmed stable ID and a new slot/lifetime; retain selected payload. A different strong identity at the same address must not merge.
- TRANS-ID-003: all modified field types and copy target types install their own Dtor address cleanup before retaining a copy link. Static normal-path trace plus managed copy removal tests; real native Dtor dispatch and CopyFromCharacter ordering are UNEXECUTED until runtime observation.

## Bulk rendered-equipment write (2026-09-03)

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-BULK-RENDERED-001 | Bulk Apply/Restore preserves game-owned armor while changing displayed Human equipment | Exercise the production armor writer with TEST_ONLY native Human/setter vtable. Backing A differs from rendered B. Apply X with explicit zero armor/stains, then Y, then Original; emulate queued slot consumption. Include unchanged visible slots and false setter returns. Run normal Bulk service sequencing with a rendered-only memory double | Exact requested values reach each changed slot once; unchanged slots invoke none. EquipmentModelIds stays A through Apply/Restore. No retry/fallback/readback gate. Original retention and source semantics remain. Facewear/hat/visor remain outside this armor-only fix. Real Minion Mirage-enabled Apply/Restore and no correlated reapply require User/runtime observation. |

## Mixed model/Bulk original outfit (2026-09-03)

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-BULK-ORIGINAL-001 | First Bulk override after Model Apply preserves game-owned Original; switching models/Bulk outfits cannot replace it | Normal BulkOutfitService StartApply/ProcessNextFrame/StartRestore with TEST_ONLY IOutfitMemory holding separate backing A and rendered NPC B. Change Bulk to X, rendered model to C, Bulk to Y, Restore; record actual memory writes. Compare unmanaged first capture. Make first backing capture unavailable, then separately unavailable after an Original already exists. Exercise managed unequip preserving current rendered metadata | Managed first Original is A, unmanaged Original remains rendered pre-Bulk; Refresh Source remains rendered. Existing Original is unchanged by later model/Bulk operations and Restore writes Original to rendered equipment and clears override; backing A remains unchanged per AM-BULK-RENDERED-001. Unavailable first original produces no write/store; existing Original needs no new backing capture. No observed content determines Apply success or supplies inferred baseline, new retry, automatic restoration, or native callback. Runtime mixed-sequence/user-visible restoration remains UNEXECUTED until User confirms. |

## Static preview and search-list performance (2026-09-03)

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-PERF-001 | Repeated static views avoid projection work without changing projected output; full list remains searchable/selectable while only visible rows are submitted | Exercise the production projector cache with a counting TEST_ONLY scene: identical requests, yaw/pitch/zoom changes, viewport move/resize, new scene, clear. Compare each changed result to uncached Project. Trace MainWindow's actual call, all filter fields/source/language, and ImGui clipper Begin/Step/index range/Destroy. User scrolls top/middle/end, selects models, changes each filter, rotates/zooms/resets/moves/resizes preview, switches tabs and reopens window | Identical view returns the same triangle list without source enumeration; each changed key and clear recomputes exact same ordered triangles/colors/UVs as Project. New scene cannot reuse prior geometry. No model rows removed/reordered and every visible row keeps its original selectable ID/actions. Existing texture order, resolution, native Apply/Restore unchanged. Visual correctness and real frame-time improvement are not inferred from managed PASS; report them separately. |

## Invalid Human search candidates (2026-09-03)

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-SEARCH-001 | Invalid Human basic identity is excluded during search registration; valid same-name candidates remain | Invoke actual `ShouldRegisterModelSearchAppearance` with TEST_ONLY payloads for observed #18950 (race 0/sex 0/tribe 0) and #18981 (1/0/2); valid race/tribe pairs 1-8 for both sexes; invalid zero/out-of-range/mismatched identities and short Customize; valid naked Human ModelChara 0, zero/sentinel armor, and unusual Body Type. Retain existing partial-Demihuman/model-only-Monster tests. Trace the predicate's normal ENpc/BNpc callers; User inspects Alkyoneus results on the deployed DLL | #18950 absent, #18981 eligible; malformed Human identity excluded, valid identity retained independently of gear or Body Type. No name blacklist, preview-result gate, target condition, or Apply-path change. Focused tests/build/static review are separate from real list/UAT. Do not ask User to reapply #18950. |

## Actor-list modification indicator (2026-09-03)

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-UI-001 | Player and NPC rows indicate model Apply or changed Bulk Outfit using the existing green text; row and modified/unapplied filters agree | Inspect actual MainWindow list/filter calls and Plugin predicate, successful Apply publication, Restore clearing, and the existing Bulk Outfit comparison. Build the actual plugin. In the real UI, for player and NPC: observe an unmodified row, Apply a different model, select modified/unapplied filters, Restore; separately change Bulk Outfit and restore it; check an already pinned actor. No test doubles stand in for visual evidence. | Current resolved Representation managed by successful model Apply OR existing Bulk Outfit modified comparison makes the row green and includes it in modified, excludes it from unapplied. Restored/unmodified rows use normal text; pinned orange and pinned filter retain precedence. Failed Apply does not create a managed marker. No historical other-Representation marker, new state/cache, native call, diagnostic gate, or automatic action is introduced. Build/static review and User visual acceptance are reported separately. |

## Equipment switch regression (2026-09-03)

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-ONESHOT-040 | Each selected outfit must reach its Create transaction without being treated as game-owned base armor or retaining a previous NPC outfit | Exercise actual `SubstituteBacking` and `InvokeWithTemporaryCreateBuffers` for equipment from `1017753` -> `1008132` -> `1030676` -> `1017753`. Use distinct A backing; a TEST_ONLY slot consumer can replace input with previous equipment only when its input matches backing, reproducing the identified interference boundary. Do not normalize `0xFFFFFFFF` or zeros. Native runtime remains the User's ordinary Model Search Apply | For every operation all ten source values reach Original once, native-setup input remains the selected array in the modeled interaction, A backing remains unchanged, and argument buffers are restored. The old backing write must fail. Existing non-null Create success and consumer/diagnostic non-authority remain unchanged. Runtime/visual PASS requires each selected body and outfit, with no previous-model equipment; immediate unloaded raw slots and synthetic tests do not prove this. No automatic retry, fallback, post-Create repair, or new callback is allowed. |

## Consecutive Apply regression (2026-09-03)

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-ONESHOT-039 | Repeated Model Search Apply must deliver selected Customize and equipment without changing game-owned Customize into a cached base appearance | Use actual `SubstituteBacking` and `InvokeWithTemporaryCreateBuffers` with an allocated test Character, original race 5/body 1, selected race 2/body 4; perform B then B and B then distinct B2. A TEST_ONLY stateful pre-Create observer models the existing base-equality/cached-customize interaction; the delegate is not FF14 or Glamourer. Separately the User performs two normal Applies on the reviewed DLL, CMC experiment disabled and Glamourer unchanged | Throughout both helper calls, Character backing Customize remains A. Each Original receives exact selected Customize and all ten equipment values once, including zeros; source argument pointers and restoration are preserved. Old backing substitution must fail the modeled observer case. Runtime/visual PASS requires both selected bodies and outfits, without mixed A/B/previous payload, new callback, diagnostic gate, automatic correction, or reapply. Existing restore cleanup is unchanged. Static review checks helper use in the normal detour; managed tests alone cannot establish actual hook-chain or visual success. |

## Apply input observation regression (2026-09-03)

AM-ONESHOT-037 also covers the escaped review finding: the actual production `DiagnosticLogEntry` must have identical `Outcome` and `properties.callResult` for `NonNull`, `Null`, and pre-call `NotCalled`. Build the entry through the normal production entry builder, with no native outcome/state update used to fix the diagnostic label. The normal writer attaches the existing operation ID so consecutive Apply observations can be correlated. No extra native call, retry, or change to finally restoration is permitted.

| ID | Requirement and path | Actions and permitted doubles | Oracle and evidence |
| --- | --- | --- | --- |
| AM-ONESHOT-036 | Locate loss of selected equipment without treating pending native loads as failed Apply | Exercise the production diagnostic property builder with distinct requested, loaded, and native-getter equipment values, including zero; exercise the production native getter reader with a TEST_ONLY unmanaged getter stand-in | All three value sets remain separate; native-getter matches never overwrite loaded observations or decide Apply outcome. The stand-in proves reader wiring only, not actual FF14 loading. |
| AM-ONESHOT-037 | Observe exact Create argument/restoration boundaries without changing the requested operation | Exercise production argument capture inside the existing temporary-buffer helper before/after its Original delegate; delegate returns non-null with intentionally different data, or throws | One Original call, unchanged return/exception, unchanged pointer identity, all original buffers restored. Before/after observations expose changes and explicit zeros without rejection, correction, or another call. Static review verifies the real detour invokes the new observations inside its existing delegate, before its finally restoration. |
| AM-ONESHOT-038 | Distinguish armor arrival from other consumer completeness | Exercise existing production DispatchArmor for matching and nonmatching owners during and after Create, then end the transaction; inspect separate consumer observations and reuse the unchanged non-null-Create success cases | Armor slot mask and consumer flags reflect actual substitution; calls are forwarded once. Missing weapon/hat/visor observations do not imply missing armor and do not change Apply success. No transaction survives End. Runtime: one User Apply on the reviewed DLL must correlate all phases under the same operation and report native input versus loaded state; no synthetic case proves the visual repair. |

## Current Apply and continuity contract

The normative source is APPEARANCE_APPLICATION_SPEC.md, including the latest 2026-09-03 all-Actor request. Explicit Model Search Apply supplies the selected payload once through the existing synchronous DisableDraw -> EnableDraw/Create transaction. Temporary native/backing inputs are restored in finally. Non-null scoped Create determines success and C publication; payload comparisons, pointer-value comparisons and consumer completeness observations do not reject, skip or fail Apply. No Verify, timeout, retry, rollback, fallback, second Create or post-Create repair is added.

A successful model operation records its payload for that Actor; a successful Bulk operation changes only outfit-owned fields and preserves the first Bulk Original. Registry/UI publication remains shared for each exact logical Actor and Representation. Natural creation consumes the Actor's retained changes, including last successful operation ordering, under TRANS-001..006 and TRANS-ID-001..003. Explicit Restore removes only the selected Actor's retained changes. The former local-only/latest-C/ARM completeness policy is retired. No per-frame appearance enforcement is introduced. New safety measures: none.

Existing pre-queue target identity, login/territory and pending-operation boundaries remain unchanged. Explicit Apply/backing/consumer and Bulk Original/rendered-write regressions remain active for these unchanged predicates; historical transition cases are not current acceptance. Managed doubles isolate only the actual state/input helpers and cannot prove native execution, rendering or User acceptance.

| ID | Linked requirement / failure and normal product path | Preconditions and input | Exact actions | Oracle, objective pass/fail, and required evidence | Cleanup |
| --- | --- | --- | --- | --- | --- |
| AM-ONESHOT-001 | R-SNAP-01 — Apply omitted selected data or retained B as a continuing override | Actor A is available; select typed payload B | Press Apply once and process the redraw to completion | One `DisableDraw` and one synchronous `EnableDraw` use complete B. Required backing values are substituted only for that call and restored in `finally`. B is not retained as an immutable source row | Press Restore if runtime testing is authorized |
| AM-ONESHOT-002 | R-SNAP-02 — a later `{user, game, successful Bulk Outfit}` change was overwritten by the original NPC payload | AM-ONESHOT-001 completed; parameterize one later input as a user change, game change, or successful Bulk Outfit with a distinct equipment-owned result | For each input, allow the next normal `ActorRegistry` Framework update to run without changing Representation | The publisher publishes actual latest C for all three inputs and B remains non-authoritative. For successful Bulk Outfit, only armor/accessories, Facewear, hat visibility, and Visor may change; `ModelCharaId`, category, `Customize`, mainhand/offhand, and Scale remain current values from the same Actor/Representation. Publication adds no retry, rollback, or redraw | None |
| HISTORICAL / SUPERSEDED AM-ONESHOT-003 | R-SNAP-06 — territory, cutscene, or GPose reapplied B or lost later player changes | AM-ONESHOT-001 completed; change one or more fields so the current state is actual C | Allow the next normal publication, then recreate the local-player representation through each supported context | `ActorRegistry` / `ActorSnapshot` publishes C from the current source Representation; transition consumes that same publication and transfers it exactly once. It never looks up or reapplies B, and it does not write the same Representation twice | Leave the actor in the latest state |
| AM-ONESHOT-004 | Restore depended on saved pre-apply data or retained carry after an explicit Restore | An Actor Morpher appearance has been applied; parameterize payload-free redraw as success/failure | Press Restore | Actor Morpher clears only the selected canonical Actor's retained model/outfit and pin state, retaining shared hooks for other modified Actors, then performs one payload-free `DisableDraw -> EnableDraw`. The game regenerates from the game-owned backing restored after Apply; no saved Base snapshot or Model Search payload is read. A redraw failure reports failure but does not restore any cleared carry | None |
| AM-ONESHOT-005 | UI policy prevented an explicit action | A selected model and target actor exist while an earlier status or retained-history flag is absent | Press Apply or Restore | The button remains callable; history availability and completed-operation state do not disable or suppress the explicit action | None |
| AM-ONESHOT-006 | R-SNAP-01 — Apply omitted or rejected part of the selected NPC payload | A registered Human Adult, Human Young, Demihuman, or Monster typed payload B contains its source components, including zero values and independently present Demihuman Customize or Equipment | Press Apply once | During the explicit target-Actor Apply call, every component present in B is supplied without ModelChara, BodyType, pointer-identity, completeness, normalization, or readback gates. Explicit zero values remain active | None |
| AM-ONESHOT-007 | An explicit unequipped slot retained the player's equipment | Complete Human payload B contains one or more zero-valued equipment slots such as Head `e0000` | Press Apply once | Each zero-valued slot is written as the current equipment value and the recreated Human visibly clears it | None |
| AM-ONESHOT-008 | R-SNAP-05 — Actor details showed backing equipment instead of the latest visible Actor | Human payload B is visibly applied while the player's backing outfit A remains unchanged; the current actor has distinct mainhand, offhand, and Facewear values | Allow the normal publication, open the Actor tab, and select the affected actor | All ten displayed armor/accessory slots, mainhand, offhand, and Facewear come from the same current published snapshot, not backing A or a UI cache. After successful original-player regeneration they equal the recreated current player state | None |
| AM-ONESHOT-009 | Actor Restore returned the body but left an outfit copied from a different actor, or a second click cancelled the first Restore marker | Apply NPC appearance B to actor A; then use Bulk Outfit to copy another actor's outfit C to A, with the pre-Bulk outfit recorded as D; parameterize D restore as success/failure | Press Restore once; while its Bulk restore is pending, press Restore again | The second request alone is rejected and does not remove the first marker. Actor Morpher keeps all carry only when the first Bulk `Original` D fails. After D succeeds, it clears only the selected canonical Actor's retained model/outfit and pin state, retaining shared hooks for other modified Actors, then performs one payload-free `DisableDraw -> EnableDraw` from game-owned backing. A redraw failure does not reinstate carry. No saved Model Search payload is read | None |
| AM-ONESHOT-010 | R-SNAP-02 — Bulk Outfit either left the generated Human unchanged or removed its modified-state record after a same-frame rendered-array mismatch | Apply Human appearance B to the local player; choose an outfit C copied from a different actor; include the local player in matching targets | Press Apply to Matching Actors once, then inspect the visible actor and Actor list | Changed armor slots are sent to the current generated Human setter; DrawData armor backing remains unchanged per AM-BULK-RENDERED-001. The visible actor shows C without changing body, Customize, hair, or scale, and the successful override remains present so the Actor name uses the modified-state color and the Outfit Modified filter includes it. A same-frame `Human.EquipmentModels` mismatch alone does not roll back a visibly applied operation | Press Actor Restore for cleanup |
| AM-ONESHOT-011 | R-SNAP-02 — Refresh Source must use current rendered outfit; mixed Model/Bulk Original must preserve original equipment | Local player backing differs from generated Human outfit C | Press Refresh Source, then start one Bulk operation | Source Outfit equals rendered C in all ten slots, facewear, hat, and visor. First Bulk Original for an appearance-managed target uses backing A per AM-BULK-ORIGINAL-001; unmanaged pre-Bulk Original stays rendered. Existing Original is retained | Restore the Bulk operation if applied |
| AM-ONESHOT-012 | Actor name color used operation history or an invented baseline | Case 1 has Bulk `Original` D and current rendered outfit C. Case 2 has no Bulk `Original`. | Open Actor list | Case 1 is green only when C differs from D and white when equal. Case 2 remains white regardless of Model Search or game changes because no baseline is invented. A pinned actor uses the existing yellow state | Restore modified actors for cleanup |
| AM-ONESHOT-013 | R-SNAP-01 — Young NPC generation received only part of the selected payload | Local player is a visibly distinct race; select an Alisaie row whose typed payload has `BodyType=4`, all required equipment values including explicit zeros, weapons, Facewear, hat visibility, Visor, and Scale | Press Apply once through Model Search; when runtime is separately authorized, inspect the directly visible result and then allow the next normal Framework publication | The single transaction supplies B to its reached normal consumers, a non-null requested Create returns terminal success, and that B becomes initial C. Consumer arrival/completion is passive observation only. Runtime/visual PASS additionally requires the actor to visibly match the selected Young Alisaie body and complete outfit. No diagnostic or post-Create readback is an oracle, and no retry, Verify, rollback, fallback, redraw, or automatic reapply occurs | Press Restore only if separately authorized |
| AM-ONESHOT-014 | Actor Morpher converted source Scale or failed Apply because game readback differed | Parameterize source Scale as positive, zero, non-finite, or absent and parameterize game readback independently | Process one Apply | The exact present source value is sent once and is not normalized; absent remains absent. Apply completion does not compare readback with the request and performs no Verify, timeout, retry, rollback, fallback, or automatic redraw | None |
| AM-ONESHOT-015 | Applying Astrid or another Special Human crashed while generated-model repair or repeated redraw logic was active | Debug plugin uses the repaired normal path; select Astrid `EventNpc#1027174` or a stable Alisaie target; no additional reproduction is required after a crash | Inspect the fixed native path, build once, then perform at most one separately authorized runtime Apply | Static PASS requires one temporary-backing creation transaction with no post-Create equipment/weapon/Facewear repair, second Create, redraw, Verify/timeout/retry/rollback. All-Actor natural recreation is separately covered by TRANS-001..006 and TRANS-ID-001..003. Runtime PASS additionally requires one visible correct Young NPC and no crash, disappearance, or T-pose | Stop immediately on any native failure |
| AM-ONESHOT-016 | Valid Human rows disappeared because their localized name or `NpcEquip` reference was absent | Human ENpc/BNpc source has a valid ModelChara and complete 26-byte Customize; localized name is blank or `NpcEquip` is absent | Build Model Search entries | Blank names receive a Source/row fallback. Missing `NpcEquip` produces ten explicit zero equipment slots, zero weapons, and visor off. Only a missing Human Customize payload excludes the row | None |
| HISTORICAL / SUPERSEDED AM-ONESHOT-017 | R-SNAP-06, R-SNAP-07 — transition state kept a separate C, controlled the same Representation, or reverted C to B | Apply B, change the player to actual C, then inspect state and trigger territory, cutscene, and GPose representation changes | Inspect the normal publisher path and observe each transition | `ActorRegistry` / `ActorSnapshot` is the appearance-value publisher on normal Framework updates. Transition retains only transferred Representation keys/one-shot ownership, consumes only the latest publication for the current source Representation, and transfers C once only to a different local-player Representation. Territory distinguishes field recreations even when object IDs are reused. It never writes the current Representation or looks up B | Restore |
| AM-ONESHOT-018 | Model Search displayed entries that Apply later rejected for missing, zero, or category-specific components | Build entries from referenced Human/Demihuman/Monster sources and ModelChara-only Monster sources | Inspect every registered entry and invoke its normal category Apply path | Every displayed entry has a constructed typed payload and Apply performs no second admission. Unconstructable sources are absent. Demihuman preserves each available component independently; model-only Monster remains applicable with Scale absent rather than inferred | None |
| AM-ONESHOT-019 | Apply lifecycle changed unrelated pin or Bulk state | Parameterize terminal outcome as queued, rejected, failed, and succeeded; seed a pin plus Bulk `Original` and `Desired`; for `Failed`, begin one requested native call; for rejected, keep a first Apply pending | Invoke the normal Apply request once for each outcome | Pin, Bulk `Original`, and Bulk `Desired` are byte-for-byte unchanged in all four outcomes. The rejected second request does not cancel or replace the first. A terminal native failure occurs only after the requested call starts and is distinct from the pre-queue rejection in `AM-ONESHOT-027`. Terminal success does not write a separate transition C; the next normal publisher refresh reads actual C | None |
| AM-ONESHOT-020 | Model Search displayed queue acceptance or another Actor's terminal result as successful application | Select valid models for Actors A and B; queue both before A finishes; parameterize each redraw terminal result as success/failure | Press Apply for A and then B, observing B's status before and after each `OperationCompleted` | Each queue acceptance is pending/neutral. A's terminal result does not change B's pending status. Green success or red failure appears only when the terminal result carries B's exact queued OperationId | None |
| HISTORICAL / SUPERSEDED AM-ONESHOT-021 | R-SNAP-06, R-SNAP-07 — a later Representation received selected B instead of the player's latest published state C | Apply B; change the player to C; allow a normal publisher refresh; enter a field, cutscene, or GPose as a different Representation | Trigger the normal Representation change | The new Representation receives the latest published C once. Actor Morpher does not write the same Representation, retain a separate appearance-value copy or native pointer, look up B, use backing A or a past C, partially assemble values, infer, reverse-map, retry, roll back, or fall back | Restore |
| AM-ONESHOT-022 | Bulk Outfit automatically wrote the captured current outfit after Apply or Restore failure | Parameterize an actor write as exception or unsuccessful result | Run one explicit Bulk Apply, Unequip, or Restore | The requested write is attempted once. Failure records the terminal result and advances without writing the captured current outfit back to the actor | None |
| HISTORICAL / SUPERSEDED AM-ONESHOT-023 | Failed territory outfit persistence entered a retry loop | A local-player desired outfit exists; change territory; make the first explicit persistence write fail | Process later Framework updates | Exactly one territory-change reapply is attempted. Failure clears pending reapply and no timer or repeated attempt is scheduled. Pin monitoring remains unchanged | None |
| AM-ONESHOT-024 | Model Search build failure was cached as a valid empty model list | Make the existing sheet-build path throw once | Request Model Search entries | The existing error path reports the failure, but no synthetic empty list is stored as the language cache | None |
| AM-ONESHOT-025 | Failed Pin enforcement was retried by the timer while the observed outfit had not changed | Pin desired outfit P; make one Pin write for observed drift C fail; keep the actor at exactly C | Process later Pin scans, then change the observed outfit to distinct D | No later scan retries C. Pin remains configured and monitoring continues. Distinct D is one new drift event and may cause one new write attempt; no failed actor state is written back | Unpin if runtime testing is authorized |
| AM-ONESHOT-026 | Explicit Bulk cancel permanently blocked Actor Restore or stopped Pin monitoring | Start a single-actor Bulk-original Restore or Pin reapply, then request Cancel before that actor is processed | Process the cancellation frame and invoke the next explicit Restore or Pin scan | The unprocessed actor receives one terminal unsuccessful completion callback without an actor write. Restore's in-flight marker is released. Pin retains the failed observed value so the same value is not timer-retried; a distinct later value can be monitored | None |
| AM-ONESHOT-027 | R-SNAP-09 — an existing pre-queue boundary started a native call or changed unrelated state | Normal Model Search Apply request before queue/native execution | Parameterize one condition as `{current target identity lost, logged out or territory unavailable, same actor already pending}`; seed pin, Bulk, and a published shared snapshot, and retain the first request for the pending condition | Request Apply once for each condition | PASS only when the requested native call does not start; the rejected request leaves pin, Bulk, and shared snapshot unchanged, and the first pending request continues unchanged when present. It adds no retry, clear, cancellation, or replacement. These are the existing pre-queue boundaries only; terminal native exception/failure after a call starts is `AM-ONESHOT-019`, not a new rejection condition | None |
| AM-ONESHOT-028 | R-SNAP-01 — the synchronous Apply reported success and published C although its scoped creation hook was never reached or `CharacterBase.Create` returned null | Normal Model Search Apply from `NativeDrawObjectInjector.Invoke` through the scoped `CreateCharacterBaseDetour`, consumer transaction, and terminal publisher | A valid typed B and current target; parameterize the same synchronous operation as `{hook not observed, hook observed and native result null, hook observed and native result non-null}` | Execute one requested operation per parameter and inspect its direct native outcome; statically trace that outcome through `NativeRedrawBackend`, `RedrawCoordinator`, and the terminal publisher | A managed boolean/result double may isolate the Create prerequisite. It does not prove hook wiring, actual native Create in FF14, native memory, or visible output | Hook-not-observed and null Create are terminal failure and publish no C. A non-null Create is terminal success and permits C publication regardless of consumer arrival/completion observation in `AM-ONESHOT-032`/`033`. No post-Create repair call, generated-model-type or appearance inspection, rendered equality comparison, Verify, timeout, retry, fallback, rollback, automatic redraw, or second call is introduced | None |
| AM-ONESHOT-029 | R-SNAP-01 — a first Alisaie Apply B1 succeeded but a later distinct Alisaie Apply B2 on the same Representation was already mixed at Create return | `CreateCharacterBaseDetour` original Customize/Equipment buffers -> one native `CharacterBase.Create` per Apply -> backing restoration -> normal publication | Two distinct complete Human payloads B1 and B2; original native Customize and Equipment buffers contain distinguishable game-owned values before each call | Execute the production temporary-buffer helper once with B1 and once with B2; inside each original-call delegate record the received pointer addresses and all component bytes; after each return inspect the original buffers. Separately, when runtime authority exists, perform B1 then B2 and inspect the visible actor | The managed original-call delegate may isolate pointer identity, payload bytes, and restoration. It does not prove hook routing, native acceptance, rendering, or User acceptance | Each call receives the exact original Customize and Equipment pointer addresses. The first sees exactly B1 and the second exactly B2, with no local-player or B1 mixing; the original buffers are restored after each call. Exactly one original Create occurs per Apply and no post-Create repair call occurs. Normal consumer calls may have their inputs substituted only by the scoped transaction in `AM-ONESHOT-032`/`034`; no extra consumer call is made. There is no retry, reapply, fallback, Verify, rollback, diagnostic readback, or persistent enforcement. Runtime PASS additionally requires the Actor to visibly match B1 then B2 | Restore only if separately authorized |
| AM-ONESHOT-030 | R-SNAP-01 — temporary native-buffer injection copied an incomplete component, changed an absent category component, or failed to restore after an exception | `NativeDrawObjectInjector` temporary original-buffer transaction used by `CreateCharacterBaseDetour` | Parameterize Human with both components, Demihuman with each independently present component, and model-only Monster with neither; include all 26 Customize bytes and ten Equipment slots with explicit zero; parameterize normal return and thrown native delegate | Invoke the production helper and inspect values and pointer identity inside the delegate; after return or exception inspect the original buffers | The managed delegate is permitted only around the production helper. It does not replace the real native Create path or visual evidence | Every present payload component is written completely into its original buffer for the one call and restored in `finally`. Every absent component is untouched and its original pointer is forwarded unchanged. An exception propagates after restoration. No null-pointer fallback, rejection, skip, retry, alternate buffer, normalization, additional hook, or product-visible control is introduced. A runtime category/path with a present payload and null original pointer is a STOP requiring new evidence and authority, not a fallback case | None |
| AM-ONESHOT-032 | R-SNAP-01, R-SNAP-09 — a reached normal game consumer did not receive the selected B or invoked its original more than once | Model Search Apply -> exact operation/Representation/object-index consumer transaction -> one `DisableDraw -> EnableDraw` -> Create, armor/accessory, main/offhand, bonus/glasses, hat, and Visor consumers -> terminal publisher | Parameterize complete Human B; Demihuman B as `{Customize-only, Equipment-only, both}`; and ModelOnly Monster B with neither component; include distinguishable values and explicit zero | For each category/component set, begin one production transaction, invoke its applicable B-required callbacks once, complete `EnableDraw`, and inspect the consumer original-call arguments plus the passive completion observation | Managed callback delegates may isolate transaction routing and argument substitution. They do not prove native hook wiring, FF14 call timing, native acceptance, rendering, or User acceptance | Human Create receives ModelChara/Customize; reached Human armor/accessory, main/offhand, bonus/glasses, hat, and Visor consumers receive exactly B and each intercepted original is invoked exactly once. Demihuman independently supplies each present structural component, and ModelOnly Monster requires Create only. Absent category components cause no call. Scale is handled once by the existing path. The matching set is recorded as complete; completion is an observation only and does not decide `TryEnable`, terminal success, C publication, backing cleanup, retry, rollback, fallback, or reapply. No callback performs another native call or post-Create repair | None |
| AM-ONESHOT-033 | R-SNAP-01, R-SNAP-09 — an incomplete consumer observation turned a non-null requested Create into Apply failure or suppressed C | Same production transaction and terminal publisher as `AM-ONESHOT-032` | For Human and Demihuman `{Equipment-only, both}`, parameterize each required armor consumer as the sole missing callback while Create returns non-null; include Demihuman Customize-only to prove armor is not required; separately parameterize Create as null or absent | Complete one transaction per parameter without synthesizing a missing callback; inspect the passive completion observation and trace the requested native result through the existing terminal publisher | Managed callbacks/result doubles only; no synthetic consumer call may satisfy the oracle | A non-null Create publishes C even when a category-required consumer is absent; the incomplete value is passive observation only. Demihuman Customize-only succeeds after its exact non-null Create without armor callbacks, and a component absent from B is not required. Absent or null Create remains failure and publishes no C. The transaction performs no extra consumer call, Verify/readback, timeout, retry, rollback, fallback, automatic redraw, or automatic reapply | None |
| AM-ONESHOT-034 | R-SNAP-09 — a later B2 inherited values or completion observations from B1 | Consecutive normal Apply transactions on the same target Representation/object index | Parameterize B1 termination as `{non-null Create with complete observation, non-null Create with incomplete observation, EnableDraw exception}` and use a distinct complete B2 whose every representable consumer value differs | Run B1 through the selected termination, end/abort its transaction, then begin and complete B2 through the same production component; record all B2 callback arguments and completion state | Managed callback delegates only; runtime and visual acceptance remain separate | B2 receives only B2 and begins with no B1 value or completion observation after every B1 termination. Each transaction is destroyed at its own `EnableDraw` return or exception; B1 with a non-null Create publishes C regardless of its consumer observation, while an exception follows the existing failure path. There is no permanent source state or continuing enforcement | None |
| AM-ONESHOT-035 | R-SNAP-09 — wrong transaction identity or unrelated callback redirected another Actor or affected Apply success | Accepted target Representation -> queued operation -> exact scoped production component around one target `EnableDraw` | Active transaction has operation ID O, Representation R, runtime object index I, and target address A; parameterize completion identity with O, R, or I as the sole mismatch, each owner-associated draw-data consumer as nonmatching A/I, and accepted R1 changing to resolved R2 after Disable but before Enable | Invoke matching and nonmatching callbacks, attempt completion with each identity tuple, then exit normally or throw from `EnableDraw`; separately accept R1, complete Disable, change resolver to R2, and process the Enable step; attempt a later callback after scope end | Managed callback delegates and identity values only; no native/visual claim | Only the exact O/R/I transaction and target A receive B substitution. Each nonmatching Armor/Weapon/Facewear/Hat/Visor callback invokes its original exactly once with unchanged arguments and contributes only to a passive incomplete observation; an O/R/I mismatch cannot redirect B or alter the requested non-null Create outcome/C publication. R1 changing to R2 before Enable remains the existing target-identity failure with no Enable call and no C publication. Normal return, incomplete observation, and exception all clear the transaction before a fresh B2 or any later callback. The exception propagates after cleanup; no rollback, retry, fallback, or persistent state is created | None |

## Historical shared current-snapshot cases

| ID | Linked requirement / failure | Product path | Preconditions and input | Exact actions | Permitted doubles | Oracle, objective pass/fail, and required actual evidence | Cleanup |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AM-SNAPSHOT-001 | R-SNAP-03, R-SNAP-04, R-SNAP-08, R-SNAP-09 — a second appearance authority, a cross-Representation assembly, or an unapproved component entered the path | `ActorRegistry.CreateSnapshot -> ActorSnapshot` publish -> Actor list, Actor detail, and transition consumers | Fixed product artifact; one current Actor/Representation with distinguishable identity/category/customize, outfit, weapon, and Scale values | Trace one normal Framework update through the production source path and its consumers | Existing managed identity/outfit/weapon/Scale providers only for state/branch isolation; no synthetic published snapshot is an acceptance artifact | PASS only when the existing registry/snapshot lane is the sole appearance-value owner/publisher; it assembles identity/`ModelCharaId`/category/`Customize`, rendered armor/accessories/Facewear/hat visibility/Visor, existing-read mainhand/offhand, and rendered Scale from the same current Actor/Representation in that order. No UI or transition value copy, hook, component, native-pointer cache, or creation observer appears. Static/source evidence is insufficient for runtime acceptance | None |
| AM-SNAPSHOT-002 | R-SNAP-01, R-SNAP-02, R-SNAP-09 — requested native Create outcome was confused with rendered equality, consumer observation, or the next publication retaining B/backing A/past C | Complete selected B -> one requested native Create outcome -> terminal publisher -> next normal `ActorRegistry` publication | Target Actor/Representation exists; B is distinguishable from backing A and a later actual C | Complete or fail the requested native Create outcome, parameterize complete/incomplete consumer observation, inspect the terminal publisher, then allow the next normal Framework update | Existing managed Apply/registry providers only for state/branch isolation; no fake proves actual C | PASS only when a non-null requested Create publishes B once as initial C regardless of consumer observation, every requested native Create failure publishes no C, and the next same-Representation normal publication may replace initial B with actual latest C without retaining backing A or past C. No diagnostic record or post-Create readback participates in the result. Required runtime evidence is the actual plugin artifact's one Apply and next normal publication | Restore only if separately authorized |
| AM-SNAPSHOT-003 | R-SNAP-03, R-SNAP-05, R-SNAP-08 — Actor detail used a cache or omitted current weapons/Facewear | Published current snapshot -> Actor list/detail display | Current Actor/Representation with distinguishable mainhand, offhand, and Facewear values | Allow one normal publication, open Actor detail, and inspect its equipment values; trace the detail consumer to the published snapshot | Existing managed display/value providers only for state/branch isolation; no UI fake proves production display | PASS only when Actor detail consumes the published current snapshot and exposes current mainhand, offhand, and Facewear values without becoming a separate authority. Required runtime/visual evidence is the actual Actor detail for the same Actor/Representation | None |
| AM-SNAPSHOT-004 | R-SNAP-03, R-SNAP-06, R-SNAP-07, R-SNAP-08, R-SNAP-09 — a new Representation received B, A, a past/partial value, or more than one transfer | Latest published current source Representation -> field/cutscene/GPose different Representation | Published actual C distinct from B, backing A, and an earlier C; one normal field, cutscene, and GPose transition each | For each context, create the different local-player Representation and observe its one transition | Existing managed Representation-key provider only for state/branch isolation; no fake proves production transfer | PASS only when transition reads only the latest publication for the current source Representation and transfers that C exactly once. It retains only keys/one-shot ownership and uses no B, A, past C, partial assembly, inference, reverse mapping, fallback, retry, rollback, or same-Representation write. Required runtime evidence covers normal field, cutscene, and GPose paths. A required missing value before arm is covered by `AM-ARM-001`: it cannot transfer, cannot be relabeled `N/A`, and authorizes no new skip, clear, or fallback | Restore only if separately authorized |
| AM-SNAPSHOT-005 | R-SNAP-04, R-SNAP-09 — category `N/A` was cross-filled or a capture absence acquired new product behavior | Normal Human/Demihuman/Monster `ActorRegistry` publication | One normal current Actor/Representation for each Human, Demihuman, and Monster category, with category-specific `N/A` fields identified from the actual path | Allow one normal Framework publication for each category and inspect the published category contract | Existing managed category provider only for state/branch isolation; it cannot substitute for actual category publication | PASS only when each publication contains its current category values and category-specific `N/A` never receives a past value from another category. Required runtime evidence covers all three normal categories. A required absent value is not category `N/A`; `AM-ARM-001` covers its non-arm without any other capture-failure behavior | None |
| AM-SNAPSHOT-006 | R-SNAP-04, R-SNAP-10 — Human Model Search omitted NPC Facewear, hat visibility, or Scale, or inherited them from the target Actor | Existing `ENpcBase` / `BNpcBase` plus effective `NpcEquip` -> `HumanAppearance` -> `AppearanceData` | NPC source has Facewear row `27`, nonzero head equipment, and Scale `0.84`; target pre-Apply state has different Facewear, `HatVisible=false`, and different Scale | Build the production Human payload and inspect every field before queueing Apply | Sheet-value records or primitive builder inputs may isolate payload construction; no target-Actor value is a permitted double for NPC-owned fields | PASS only when payload has `FacewearModelId=27`, `HatVisible=true`, and `ModelScale=0.84` from the NPC source. A second source with no Facewear and zero head has `FacewearModelId=0` and `HatVisible=false`. Missing source data is not filled from the target Actor, `0`, `false`, `1.0`, or another row by inference | None |
| AM-SNAPSHOT-007 | R-SNAP-03, R-SNAP-08, R-SNAP-10 — Apply success discarded complete NPC C or the next refresh replaced it with target backing A | Model Search terminal success/failure -> existing applied appearance state -> `ActorRegistry` / `ActorSnapshot.CurrentAppearance` -> selected Representation UI/transition | Complete NPC C is distinguishable from target backing A in ModelCharaId, Facewear, hat visibility, and Scale; NPC has a nonzero head and target backing has hidden headgear | Complete one requested Apply successfully, publish its C for the exact Representation, then process a refresh whose backing values are A; separately exercise rejected and ordinary failed Apply | Existing managed service/registry providers may isolate state transitions; no synthetic result proves runtime appearance | PASS only when successful Apply publishes the exact complete NPC C, the next refresh does not replace NPC-owned ModelCharaId, Facewear, hat visibility, or Scale with A, and Actor list/detail/transition obtain the same C instance for the same selected Representation. Rejected and failed Apply publish nothing. No post-Create equipment write, new component/cache/observer, target-field inheritance, fallback, retry, rollback, or extra redraw appears | Restore only if separately authorized |

## Historical / superseded ARM_AFTER_COMPLETE_C cases

| ID | Linked requirement / failure | Product path | Preconditions and input | Exact actions | Permitted doubles | Oracle, objective pass/fail, and required actual evidence | Cleanup |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AM-ARM-001 | R-ARM-01, R-ARM-03, R-SNAP-04, R-SNAP-07, R-SNAP-09 — null, category-contract partial, or unsupported current data armed a transition, reached `InjectionContext`, or substituted historical data | `CurrentAppearance -> transition arm -> InjectionContext` | One source Representation; parameterize `CurrentAppearance` as `null`, a Human snapshot that fails its exact `Complete`/length/presence row, a Demihuman snapshot that fails its exact `Complete`/equipment-length/scale row, a Monster snapshot that fails its exact `ModelOnly`/scale row, or Other/`Completeness=Unsupported`; seed distinguishable B, backing A, and old C. Category `N/A`, `ModelCharaId=0`, zero equipment, and `0`/`false` field values are valid controls when their table row otherwise passes; `SourceRowId` is not an input. Create a different Representation before a complete publication | For each null/partial/unsupported input, process the normal transition opportunity and the early different-Representation draw | Existing managed current-appearance and Representation-key providers only for state/branch isolation; no synthetic snapshot or `InjectionContext` receipt is actual-path evidence | PASS only when every table-failing/null/unsupported input does not arm and does not reach `InjectionContext`; valid `0`/`false` and category `N/A` controls are not relabeled missing. The early different Representation may temporarily display the game's native appearance and receives no B, backing A, old C, partial assembly, inference, reverse mapping, or fallback. The path adds no automatic re-Apply, same-Representation injection/reinjection, timeout, retry count, old-C retention, terminal drop, clear, reject, or rollback. Required actual evidence is the normal product path and observable early Representation; a double cannot replace it | Leave the early Representation uninjected; no new cleanup behavior |
| AM-ARM-002 | R-ARM-02, R-ARM-03, R-SNAP-04, R-SNAP-06, R-SNAP-07, R-SNAP-09 — C1 was delayed until C2, a later partial publication retained C1, or duplicate/early transfer reached a Representation | Same-source normal `ActorRegistry` / `ActorSnapshot` publication -> transition arm -> next different Representation -> `InjectionContext` | Start from `AM-ARM-001`; parameterize Human, Demihuman, and Monster C1/C2 snapshots that each exactly satisfy their respective table row, including valid `ModelCharaId=0`, zero equipment, and `0`/`false` controls where applicable, with category `N/A` only where the row defines it and no `SourceRowId` criterion. C1 and C2 are distinguishable from B, backing A, and each other | For each field/cutscene/GPose context: publish complete C1 from the same source Representation, create the next different Representation and process a duplicate observation; publish null, a table-failing partial, and `Completeness=Unsupported` in parameterized runs, then create a new different Representation; publish complete C2 from that same source, create the next new different Representation, and process its duplicate observation. Observe that the early key from `AM-ARM-001` is not re-targeted | Existing managed publisher and Representation-key providers only for state/branch isolation; no fake publication, weapon read, or injection receipt proves actual transfer | PASS only when complete C1 arms immediately without waiting for C2 and the first next different Representation reaches `InjectionContext` with C1 exactly once; each duplicate observation produces no second transfer. After a same-source null/partial/unsupported latest publication, the next new Representation does not arm or reach `InjectionContext` and does not use C1. Complete C2 then arms again and only its next new different Representation reaches `InjectionContext` with C2 exactly once. The early Representation and the unarmed intervening Representation are never injected or reinjected. Every destination reads only the latest publisher snapshot; B, A, old C, partial assembly, inference, reverse mapping, fallback, a second C authority, automatic re-Apply, timeout, retry count, terminal drop, clear, reject, or rollback do not appear. Required runtime/visual evidence covers actual field, cutscene, and GPose paths | Restore only if separately authorized |

## Diagnostic cases

| ID | Linked requirement / failure and normal product path | Preconditions and input | Exact actions | Oracle, objective pass/fail, and required evidence | Cleanup |
| --- | --- | --- | --- | --- | --- |
| AM-DIAG-001 | Reapplying Full diagnostic settings failed because the old writer still locked `latest.jsonl` | One router has an active Full writer for a fixed session ID | Write and flush one event, switch to Full again with the same session ID, then write and flush a second event | The second switch returns `true`, no `IOException` occurs, and the replacement `latest.jsonl` contains the second event | Dispose router |

`AM-DIAG-002` and its A/B/C JSONL observations are retained only as historical defect evidence. They are not current test coverage, are outside this experiment's write and execution scope, and do not authorize or satisfy any Apply, transaction, publication, runtime, or visual acceptance predicate.

## Historical appearance and scale cases

Historical cases above are retained solely as incident context, not current transition acceptance or PASS evidence. Current transition coverage is TRANS-001..006 and TRANS-ID-001..003; the explicit Apply/backing/consumer and Bulk Original/rendered-write regressions remain active only for their unchanged operation predicates. Runtime and visual acceptance require the actual normal product path and are reported separately.


## Actor registry cases

| ID | Linked requirement / failure and normal product path | Preconditions and input | Exact actions | Oracle, objective pass/fail, and required evidence | Cleanup |
| --- | --- | --- | --- | --- | --- |
| AM-ACTOR-001 | Summoned minions were omitted before reaching the Actor list | Supported-kind predicate receives `ObjectKind.Pc`, `ObjectKind.EventNpc`, `ObjectKind.BattleNpc`, and `ObjectKind.Companion` | Evaluate each kind through the production registry predicate | All four kinds are accepted; in particular `Companion` is not discarded before snapshot creation | None |
| AM-ACTOR-002 | Expanding the registry could admit unrelated game objects | Supported-kind predicate receives representative unsupported kinds such as `ObjectKind.None` and `ObjectKind.Treasure` | Evaluate each kind through the production registry predicate | Unsupported kinds remain rejected | None |
| AM-ACTOR-003 | Managed eligibility does not prove the live minion is displayed | Debug plugin loaded; local player has normally summoned one minion | Open the Actor list and locate the summoned minion | The minion name appears as one selectable Actor entry and its details report `ObjectKind.Companion`; final visual verdict belongs to the user | Dismiss the minion if desired |
| AM-ACTOR-004 | Actor list must prioritize only the local player's own minion without changing filters or morph behavior | Managed Actor entries include the local player, a `Companion` whose Address equals local `Character.ChildObject`, a different-address `Companion`, and other supported entries | Evaluate the production ownership predicate, then sort the supplied entries through the production display-order helper | Local player is first; only the matching `Companion` is second; the different-address `Companion` remains in the existing `ObjectKind`/name-ordered remainder. Sorting considers only the supplied entries and does not bypass an Actor-list filter or invoke a minion apply path. | None |

## Bulk Outfit filter persistence cases

| ID | Linked requirement / failure and normal product path | Preconditions and input | Exact actions | Oracle, objective pass/fail, and required evidence | Cleanup |
| --- | --- | --- | --- | --- | --- |
| AM-BULK-FILTER-001 | Existing configurations omitted the new fields | Deserialize a configuration with `Version = 5` and no Bulk Outfit filter properties | Run `MigrateAndValidate(false)` | `Version` advances to the new schema version; Include Yourself and Exclusion Enabled are `false`; both filter sets retain index `0` and empty Name defaults | None |
| AM-BULK-FILTER-002 | Target and exclusion selections were lost after restart | Set every persisted Target Filters and Exclusion Filters property to distinct valid non-default values | Serialize, deserialize, then run `MigrateAndValidate(false)` | Include Yourself, target Actor Type/Race/Gender/Age/Name, Exclusion Enabled, and exclusion Actor Type/Race/Gender/Age/Name equal the original values | None |
| AM-BULK-FILTER-003 | Corrupt or stale integer selections could index `MainWindow` arrays out of range | Load negative and oversized Actor Type, Race, Gender, and Age values for both filter sets | Run `MigrateAndValidate(false)` | Every resulting selection is within its UI range: Actor Type `0..2`, Race `0..8`, Gender `0..2`, Age `0..2`; Names and enabled flags remain usable | None |
| AM-BULK-FILTER-004 | A local player's backing Viera-female Customize could make a rendered Hyur-female Human fail its matching Bulk Outfit target filter | Construct a local `Pc` snapshot with backing Race `8`, Gender `1`, and normal BodyType; transform it through the production visible-Human customize helper to Race `1` Hyur, Gender `1` Female, and the same BodyType. Use target ActorType `All`, Race `1`, Gender `1`, Age `All`, empty Name, Include Yourself `true`, and no Exclusion. | Resolve the local actor through the production `BulkOutfitTargetResolver`. | The local player's key is in `EligibleTargets`. Include Yourself admits the local player but does not bypass the Hyur-female target filter; Exclusion semantics are not changed or retested by this case. | None |

## Command registration cases

| ID | Linked requirement / failure and normal product path | Preconditions and input | Exact actions | Oracle, objective pass/fail, and required evidence | Cleanup |
| --- | --- | --- | --- | --- | --- |
| AM-CMD-001 | A duplicate instance removed commands it never registered | First instance owns a command; second instance observes it occupied | Initialize then dispose the second registration lease | Second instance never calls Add or Remove and the first handler remains registered | Dispose first lease |
| AM-CMD-002 | Commands stayed missing after the owning instance unloaded | Second instance initially observes an occupied command, then the first owner removes it | Run the remaining instance's next registration check | Remaining instance calls Add once, records ownership, and later removes only its own handler | Dispose remaining lease |
| AM-CMD-003 | Normal lifecycle must remain unchanged | Command is initially free | Register, repeat the registration check, then dispose | Add runs once, repeated checks do nothing, Remove runs once | None |
| AM-CMD-004 | Repository update plus Dev Plugin reload made both documented commands unavailable | Repository Actor Morpher and Dev ActorMorpher are loaded; one instance is then unloaded | Enter `/actormorpher`, close the window, then enter `/amorph` | Both commands are found and each opens Actor Morpher; Dalamud log has no repeated registration error after ownership transfers | Disable the duplicate instance |

AM-BULK-RENDERED-001 Pin negative case: backing A != desired X, rendered X => the normal TryCaptureOutfit reader returns X and the existing Pin equality check does not queue reapply; rendered Y => comparison reports the actual difference. Verify the reader through BulkOutfitService with the existing memory double and inspect its sole Plugin Pin consumer. No new Pin policy, timer or retry is introduced. Live Pin execution is reported separately.
# 2026-09-03: TRANS 継続保持の回帰定義

- TRANS-001: 成功したmodel Bを保持し、元表現不在・partial/null publication・同一キーでの再Create・移動往復を経てもBを渡す。別Actorへ渡さない。TEST_ONLY store/通常のCreate入力helperで確認。
- TRANS-002: PCの個人識別子、所有者つきCompanion、配置IDつきNPCの再生成を同一Actorに結び、同BaseIdの別配置/別所有者/別プレイヤーには結ばない。CopyFromCharacterの明示source→targetとcopy-of-copyを全Actor種別で保持し、source消滅後も生存copyへ引き継ぐ。native descriptor/copy-mapを分離して確認。
- TRANS-003: Bulk Original=A、Desired=Xをterritory変更で消さず、再生成後もX、再変更YでもOriginal=A。装備のみのCreate入力はCustomize/武器/モデルIDを変えず、10装備slotと指定metadataのみ引き継ぐ。管理/native buffer helperで確認。
- TRANS-004: model B→Bulk X→model Dでは最後に成功した操作順が再生成時にも一致する。Bulk RestoreはOriginalへ戻し、Actor Restoreはmodel/outfitの保持を解除する。他Actorの保持は残る。
- TRANS-005: explicit Applyは既存1回のCreateにだけ注入する。自然再生成時も1回のoriginal呼出し、診断不一致でskip/failure/retryなし。既存NativeDrawObjectInjectorTests/NativeOutfitMemoryTestsの関連ケースを回帰実行。旧local publication gateを要求として残さない。
- TRANS-006 (実機/UAT): self/other PC/Companion/EventNpc/BattleNpcを個別変更し、テレポ往復・カットシーン入退場・再表示でmodel/装備/緑表示/Originalを確認。その後Restoreし次の再生成で変更が復活しないことを確認。未操作種別・遷移はUNEXECUTED。DLL hashとユーザー観測を紐付ける。異なるNPCの同一視は不可、ゲームに継続する個体IDがない場合の対応範囲は未検証として報告。
# Free color acceptance (2026-09-03)

- SOURCE-INITIAL-001: A newly constructed BulkOutfitService exposes ten empty armor/accessory slots, no facewear and no dyes, so the normal source table and picker are usable before Copy. StartApply consumes that displayed source, preserves first Original and does not change actors before Apply. Source item/color edits before Copy must work; successful RefreshSource replaces the source with the captured outfit. Test normal service methods using existing TEST_ONLY memory. Existing no-source behavior after failed capture remains unchanged. UI initial display/selection is user UAT, UNEXECUTED until observed.

- COLOR-006: Right-click an editable source dye swatch to clear only that channel's game stain and custom RGB (Stain=0, Color=null). White RGB remains an explicit color. Test each channel through ClearSourceDye -> StartApply -> ProcessNextFrame -> Restore with existing TEST_ONLY memory; preserve opposite channel, equipment identity, other slots, facewear and first Original. Source clearing performs no Actor Apply before the existing Apply button. UI distinguishes undyed with a blank standard button and tooltip (the user rejected the slash); read-only swatches and facewear gain no edit action. Actual right-click/rendered color observation is user UAT and UNEXECUTED until observed.

- HEAD-UNEQUIP-001: NativeOutfitMemory.ApplyRenderedEquipment receives a Head-only selection of zero with both zero and nonzero stain bytes. A TEST_ONLY native setter mutates its input and pending head to a retained hat. After the single call, pending head must equal the requested packed armor, slot 0 must be marked for update, other slot flags/values and backing must remain unchanged. Native false return must not reject, retry or change the selected value. Repeat with a nonzero replacement and original restoration; unchanged Head must not enqueue extra work. The actual product method is exercised, but native game execution is not replaced by this double.
- HEAD-UNEQUIP-002 (runtime/UAT): User right-clicks Head with their current plugin configuration, observes no head equipment, equips another Head, and uses Restore. Current/modified equipment and visible model must agree; original must restore and other equipment must stay unchanged. No automatic game operation by the agent. Record this as UNEXECUTED until observed.

- COLOR-001: Each source armor channel accepts independent RGB, including black; clearing it retains the underlying game stain. Source edits do not apply to actors until existing Apply is pressed. Test source-edit method and immutable operation capture.
- COLOR-002: Production table transformation changes only dye-enabled diffuse rows belonging to the selected channel (legacy channel 1 and modern channels 1/2). Preserve all other table values and shared source bytes. Test synthetic unmanaged material buffers, including no dye table and no matching rows; those observations do not reject equipment Apply.
- COLOR-003: Copy, outfit equality, full appearance conversion, persistence and pin serialization retain RGB. Subsequent outfit changes preserve Original; explicit Restore removes the requested color and later slot loading cannot resurrect it. Test production data paths with existing managed doubles; no GPU claims.
- COLOR-004: Native path uses actor-owned textures, original SetupSlotModel once, no polling/retry, and no stale pointer retained across destruction. Read-only review of actual callback, Apply and Dispose wiring. Build against installed SDK.
- COLOR-005 (runtime/UAT): User copies outfit, selects two distinct colors, Applies to self and Human minion, repeats with different outfit, teleports/cutscene, pins and restores. Requested dye regions retain colors and original outfit returns; unrelated actors/material regions unchanged. Record DLL identity; unperformed actions remain UNEXECUTED.
